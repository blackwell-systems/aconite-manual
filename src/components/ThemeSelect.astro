---
/*
 * Override of Starlight's `ThemeSelect`.
 *
 * The Aconite manual ships dark-mode only, so there is no light/dark toggle.
 * This component renders nothing, which removes the theme-select control from
 * the site header entirely.
 */
---
